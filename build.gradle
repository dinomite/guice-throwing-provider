apply plugin: "java"
apply plugin: "application"

repositories {
    jcenter()
}

application {
    mainClassName = "net.dinomite.guice.throwing_provider.Main"
}

dependencies {
    compile "com.google.inject:guice:4.2.2"
}
